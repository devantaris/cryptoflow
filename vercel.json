{
  "buildCommand": "cd frontend && npm install && npx rimraf tsconfig.tsbuildinfo tsconfig.app.tsbuildinfo && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
